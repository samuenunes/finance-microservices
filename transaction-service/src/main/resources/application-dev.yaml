spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/transaction
    username: asdf
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true